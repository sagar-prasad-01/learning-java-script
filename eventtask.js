function fun()
{
    let a=parseInt(prompt("enter one num"))
    let b=parseInt(prompt("enter sec num"))
    console.log("sum = " + (a+b))
}
function fun1(cl)
{
    
    document.body.style.backgroundColor=cl
}